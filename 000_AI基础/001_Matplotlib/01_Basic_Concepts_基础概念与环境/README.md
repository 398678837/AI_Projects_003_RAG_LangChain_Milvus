# Matplotlib基础概念与环境

本示例展示了Matplotlib的基本概念和环境配置，帮助初学者理解Matplotlib的架构和使用方法。

## 功能说明

- 演示Matplotlib的基本架构（Figure、Axes、Artist）
- 展示不同的Matplotlib样式
- 演示Matplotlib配置参数的修改
- 保存和显示图形

## 代码结构

```python
# 1. 导入必要的库
# 2. 定义基本架构演示函数
# 3. 定义样式设置函数
# 4. 定义配置参数函数
# 5. 主函数调用各个演示函数
```

## 配置说明

1. **安装依赖**：
   ```bash
   pip install matplotlib numpy
   ```

## 运行示例

1. 运行示例：
   ```bash
   python demo.py
   ```

2. 查看结果：
   - 会显示各个图形
   - 同时在当前目录生成对应的图片文件

## 学习要点

- **Matplotlib架构**：理解Figure、Axes、Artist的关系
- **样式系统**：如何使用不同的Matplotlib样式
- **配置参数**：如何修改Matplotlib的默认配置
- **环境设置**：如何为不同的绘图需求配置Matplotlib

## 扩展建议

- 尝试创建自己的Matplotlib样式
- 学习如何永久修改Matplotlib配置
- 探索更多的Matplotlib配置选项
- 了解Matplotlib的后端选择