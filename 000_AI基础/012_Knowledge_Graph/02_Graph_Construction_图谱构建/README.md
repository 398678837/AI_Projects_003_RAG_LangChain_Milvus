# 知识图谱构建

本示例展示了知识图谱的构建方法，包括从不同数据源构建知识图谱、层次结构构建和属性扩展等。

## 功能说明

- 从数据构建知识图谱：使用人物关系数据构建知识图谱
- 从JSON数据构建知识图谱：使用电影数据构建知识图谱
- 从CSV数据构建知识图谱：使用Python生态系统数据构建知识图谱
- 知识图谱的层次结构：构建层次化的知识图谱
- 知识图谱的属性扩展：扩展知识图谱的节点和边属性

## 代码结构

```python
# 1. 从数据构建知识图谱
# 2. 从JSON数据构建知识图谱
# 3. 从CSV数据构建知识图谱
# 4. 知识图谱的层次结构
# 5. 知识图谱的属性扩展
```

## 配置说明

1. **依赖包**：
   - networkx
   - matplotlib
   - numpy

2. **安装依赖**：
   ```bash
   pip install -r ../../requirements.txt
   ```

## 运行示例

1. 运行示例代码：
   ```bash
   python demo.py
   ```

## 学习要点

- **数据导入**：学习如何从不同数据源导入数据构建知识图谱
- **JSON数据处理**：学习如何从JSON数据构建知识图谱
- **CSV数据处理**：学习如何从CSV数据构建知识图谱
- **层次结构**：学习如何构建层次化的知识图谱
- **属性扩展**：学习如何扩展知识图谱的属性

## 扩展建议

- 尝试从更多类型的数据源构建知识图谱
- 学习使用专业的知识图谱构建工具
- 探索大规模知识图谱的构建方法
- 尝试构建领域特定的知识图谱