# Keras序列模型

本示例展示了Keras的Sequential模型的使用方法，包括基本序列模型的创建、层的添加、模型配置、训练和评估、保存和加载以及实际应用。

## 功能说明

- 基本序列模型：创建简单的序列模型
- 序列模型的层添加：通过add方法动态添加层
- 序列模型的配置：添加Dropout、正则化和BatchNormalization等层
- 序列模型的训练和评估：训练模型并评估性能
- 序列模型的保存和加载：保存训练好的模型并加载使用
- 序列模型的应用：使用训练好的模型进行预测

## 代码结构

```python
# 1. 基本序列模型
# 2. 序列模型的层添加
# 3. 序列模型的配置
# 4. 序列模型的训练和评估
# 5. 序列模型的保存和加载
# 6. 序列模型的应用
```

## 配置说明

1. **依赖包**：
   - tensorflow
   - numpy
   - matplotlib

2. **安装依赖**：
   ```bash
   pip install -r ../../requirements.txt
   ```

## 运行示例

1. 运行示例代码：
   ```bash
   python demo.py
   ```

## 学习要点

- **基本序列模型**：使用Sequential API创建简单的模型
- **层的添加**：通过add方法动态添加层到模型
- **模型配置**：添加Dropout、正则化和BatchNormalization等层来提高模型性能
- **训练和评估**：训练模型并评估其在测试集上的性能
- **模型保存和加载**：保存训练好的模型并在需要时加载使用
- **模型应用**：使用训练好的模型进行预测

## 扩展建议

- 尝试创建更深层的序列模型
- 探索不同的激活函数
- 学习如何使用不同的优化器和学习率调度器
- 尝试使用数据增强来提高模型性能
- 学习如何处理过拟合问题