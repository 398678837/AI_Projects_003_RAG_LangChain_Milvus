# FastAPI依赖注入

本示例展示了FastAPI的依赖注入功能，包括简单依赖、依赖链、全局依赖等。

## 功能说明

- 简单依赖：获取查询参数
- 依赖类：使用类作为依赖
- 依赖链：依赖嵌套
- 全局依赖：全局应用的依赖
- 安全依赖：基于HTTP Basic认证的依赖
- 依赖注入与路径操作装饰器：在路径操作中使用多个依赖
- 可选依赖：处理可选参数
- 依赖注入与响应模型：返回符合响应模型的数据
- 依赖注入与错误处理：在依赖中处理错误
- 依赖注入与后台任务：使用后台任务

## 代码结构

```python
# 1. 简单依赖
# 2. 依赖类
# 3. 依赖链
# 4. 全局依赖
# 5. 安全依赖
# 6. 依赖注入与路径操作装饰器
# 7. 可选依赖
# 8. 依赖注入与响应模型
# 9. 依赖注入与错误处理
# 10. 依赖注入与后台任务
```

## 配置说明

1. **依赖包**：
   - fastapi
   - uvicorn
   - pydantic

2. **安装依赖**：
   ```bash
   pip install -r ../../requirements.txt
   ```

## 运行示例

1. 运行示例代码：
   ```bash
   python demo.py
   ```
   或
   ```bash
   uvicorn demo:app --reload
   ```

2. 访问方式：
   - 简单依赖：http://127.0.0.1:8000/items/?skip=0&limit=10
   - 依赖类：http://127.0.0.1:8000/users/?skip=0&limit=10
   - 依赖链：http://127.0.0.1:8000/users/1?user_id=1
   - 安全依赖：http://127.0.0.1:8000/secure (需要输入用户名admin，密码password)
   - 可选依赖：http://127.0.0.1:8000/optional
   - 依赖注入与响应模型：http://127.0.0.1:8000/user-model?user_id=1
   - 依赖注入与错误处理：http://127.0.0.1:8000/validated-user?user_id=1
   - 依赖注入与后台任务：http://127.0.0.1:8000/send-email?email=test@example.com&message=Hello

## 学习要点

- **简单依赖**：如何定义和使用简单的依赖函数
- **依赖类**：如何使用类作为依赖
- **依赖链**：如何创建和使用依赖链
- **安全依赖**：如何实现基于认证的依赖
- **可选依赖**：如何处理可选参数
- **后台任务**：如何使用后台任务

## 扩展建议

- 尝试使用更复杂的依赖链
- 学习如何实现数据库依赖
- 探索如何实现缓存依赖
- 学习如何使用依赖注入进行测试
- 尝试实现更复杂的业务逻辑依赖