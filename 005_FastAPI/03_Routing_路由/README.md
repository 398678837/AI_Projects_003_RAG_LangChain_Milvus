# FastAPI路由

本示例展示了FastAPI的路由功能，包括基本路由、路径参数、查询参数、嵌套路由等。

## 功能说明

- 基本路由：根路径
- 路径参数：带路径参数的路由
- 带验证的路径参数：使用Path验证路径参数
- 查询参数：使用Query验证查询参数
- 混合参数：混合路径参数和查询参数
- 嵌套路由：使用APIRouter创建子路由
- 带请求体的路由：处理POST请求
- 路径转换器：处理包含斜杠的路径

## 代码结构

```python
# 1. 创建FastAPI应用实例
# 2. 基本路由
# 3. 路径参数
# 4. 带验证的路径参数
# 5. 查询参数
# 6. 混合参数
# 7. 嵌套路由
# 8. 带请求体的路由
# 9. 路径转换器
# 10. 启动服务器
```

## 配置说明

1. **依赖包**：
   - fastapi
   - uvicorn
   - pydantic

2. **安装依赖**：
   ```bash
   pip install -r ../../requirements.txt
   ```

## 运行示例

1. 运行示例代码：
   ```bash
   python demo.py
   ```
   或
   ```bash
   uvicorn demo:app --reload
   ```

2. 访问方式：
   - 基本路由：http://127.0.0.1:8000/
   - 路径参数：http://127.0.0.1:8000/items/1
   - 带验证的路径参数：http://127.0.0.1:8000/users/5
   - 查询参数：http://127.0.0.1:8000/items/?skip=0&limit=10
   - 混合参数：http://127.0.0.1:8000/items/1/details?q=test&category=electronics
   - 嵌套路由：http://127.0.0.1:8000/api/items/
   - 路径转换器：http://127.0.0.1:8000/files/path/to/file.txt

## 学习要点

- **基本路由**：如何定义基本的路由
- **路径参数**：如何定义和使用路径参数
- **参数验证**：如何使用Path和Query进行参数验证
- **嵌套路由**：如何使用APIRouter创建子路由
- **路径转换器**：如何处理包含斜杠的路径

## 扩展建议

- 尝试使用更多的参数验证器
- 学习如何使用依赖注入处理路由
- 探索如何实现API版本控制
- 学习如何处理WebSocket连接
- 尝试实现更复杂的路由结构